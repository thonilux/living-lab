# Data

Local data storage (indoor/outdoor sensor data, energy data, prediction data, control action logs, PMV/PPD/Tr, occupancy, questionnaire responses, timestamps).

Not tracked in git by default (see .gitignore) — raw/collected data stays local or goes to external storage.
